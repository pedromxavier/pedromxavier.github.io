function renderTab(element) {
    // let src_lines = element.innerText.split("\n");
    // let dst_lines = [];

    // src_lines.forEach(
    //     _line => {
    //         let line = _line.trim();

    //         dst_lines.push(line);
    //     }
    // );

    // element.innerHTML = dst_lines.join(` <br>\n`);

    return;
}
